import { PrimePlaceDirective } from './prime-place.directive';

describe('PrimePlaceDirective', () => {
  it('should create an instance', () => {
    const directive = new PrimePlaceDirective();
    expect(directive).toBeTruthy();
  });
});
